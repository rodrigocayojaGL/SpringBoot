type Vehicle {
	id: ID!,
	type: String,
	model: String
}

type Query {
	findAllVehicle(count: Int):[Vehicle]
	findByIdVehicle(id: ID):Vehicle
}

type Mutation {
	createVehicle(type: String!, model: String!):Vehicle
}